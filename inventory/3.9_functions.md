## 📝 Bloque 3.9 — Funciones

### Resumen de funciones por schema

| Schema | Funciones | Propósito principal |
|--------|-----------|---------------------|
| `auth` | 1 | Actualización de timestamps |
| `public` | 1 | Actualización de timestamps |
| `ticketing` | 2 | Lógica de negocio de tickets |
| **TOTAL** | **4** | |

---

### Detalle de funciones

---

#### 1. `update_updated_at()` (auth schema)

**Schema:** `auth`

**Propósito:** Actualiza automáticamente el campo `updated_at` al modificar un registro.

**Definición:**

```sql
CREATE OR REPLACE FUNCTION auth.update_updated_at()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

**Uso en triggers:**

| Tabla | Trigger | Evento |
|-------|---------|--------|
| `auth.users` | `trg_update_users` | UPDATE |
| `auth.sessions` | `trg_update_sessions` | UPDATE |

**Observaciones:**
- Función simple y eficiente
- Solo se ejecuta en operaciones UPDATE
- Usa `NOW()` para timestamp actual
- Forma parte del schema `auth`

---

#### 2. `update_updated_at()` (public schema)

**Schema:** `public`

**Propósito:** Actualiza automáticamente el campo `updated_at` al modificar un registro.

**Definición:**

```sql
CREATE OR REPLACE FUNCTION public.update_updated_at()
RETURNS TRIGGER AS $$
BEGIN
    NEW.updated_at = NOW();
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

**Uso en triggers:**

| Tabla | Trigger | Evento |
|-------|---------|--------|
| `crm.customers` | `trg_customers_update` | UPDATE |
| `ticketing.events` | `trg_update_events` | UPDATE |
| `ticketing.organizers` | `trg_update_organizers` | UPDATE |
| `ticketing.venues` | `trg_update_venues` | UPDATE |
| `ticketing.ticket_types` | `trg_update_ticket_types` | UPDATE |
| `ticketing.tickets` | `trg_update_tickets` | UPDATE |
| `ticketing.categories` | `update_categories_updated_at` | UPDATE |
| `billing.orders` | `trg_update_orders` | UPDATE |
| `billing.order_items` | `trg_update_order_items` | UPDATE |
| `billing.payments` | `trg_update_payments` | UPDATE |
| `billing.refunds` | `trg_update_refunds` | UPDATE |
| `fiscal.country_config` | `trg_update_country_config` | UPDATE |
| `fiscal.invoices` | `trg_update_invoices` | UPDATE |
| `global.supported_languages` | `trg_update_supported_languages` | UPDATE |
| `global.supported_currencies` | `trg_update_supported_currencies` | UPDATE |
| `global.system_config` | `trg_update_system_config` | UPDATE |
| `notifications.templates` | `trg_update_templates` | UPDATE |
| `notifications.messages` | `trg_update_messages` | UPDATE |
| `analytics.daily_metrics` | `trg_update_daily_metrics` | UPDATE |
| `analytics.event_metrics` | `trg_update_event_metrics` | UPDATE |
| `integration.webhooks` | `trg_update_webhooks` | UPDATE |
| `integration.api_calls` | `trg_update_api_calls` | UPDATE |

**Observaciones:**
- **Misma función que `auth.update_updated_at()` pero en schema `public`**
- Esta duplicación es innecesaria
- 22 triggers usan esta función
- Recomendación: consolidar en un solo lugar

---

#### 3. `ticketing.generate_ticket_code()`

**Schema:** `ticketing`

**Propósito:** Genera un código único para cada ticket de forma automática al insertar un nuevo registro.

**Definición:**

```sql
CREATE OR REPLACE FUNCTION ticketing.generate_ticket_code()
RETURNS TRIGGER AS $$
DECLARE
    v_event_prefix VARCHAR(3);
    v_type_prefix VARCHAR(2);
    v_random_part VARCHAR(8);
    v_attempt_code VARCHAR(50);
    v_attempt INTEGER := 0;
BEGIN
    -- Obtener prefijos del evento y tipo de ticket
    SELECT 
        UPPER(SUBSTRING(e.slug FROM 1 FOR 3)),
        UPPER(SUBSTRING(tt.name FROM 1 FOR 2))
    INTO 
        v_event_prefix,
        v_type_prefix
    FROM ticketing.events e
    JOIN ticketing.ticket_types tt ON tt.id = NEW.ticket_type_id
    WHERE e.id = NEW.event_id;
    
    -- Generar código único
    WHILE v_attempt < 10 LOOP
        v_random_part := UPPER(SUBSTRING(encode(gen_random_bytes(6), 'base64') FROM 1 FOR 8));
        v_random_part := REPLACE(REPLACE(v_random_part, '/', '0'), '+', '1');
        v_random_part := REPLACE(REPLACE(v_random_part, '=', '2'), '-', '3');
        
        v_attempt_code := v_event_prefix || v_type_prefix || v_random_part;
        
        IF NOT EXISTS (SELECT 1 FROM ticketing.tickets WHERE code = v_attempt_code) THEN
            NEW.code := v_attempt_code;
            NEW.secret_hash := encode(
                digest(
                    v_attempt_code || gen_random_bytes(32) || EXTRACT(EPOCH FROM NOW())::TEXT,
                    'sha256'
                ),
                'hex'
            );
            RETURN NEW;
        END IF;
        
        v_attempt := v_attempt + 1;
    END LOOP;
    
    RAISE EXCEPTION 'No se pudo generar un código de ticket único';
END;
$$ LANGUAGE plpgsql;
```

**Uso en triggers:**

| Tabla | Trigger | Evento |
|-------|---------|--------|
| `ticketing.tickets` | `trg_generate_ticket_code` | INSERT |

**Lógica de generación:**
1. Toma los primeros 3 caracteres del slug del evento (`v_event_prefix`)
2. Toma los primeros 2 caracteres del nombre del tipo de ticket (`v_type_prefix`)
3. Genera 8 caracteres aleatorios con `gen_random_bytes()`
4. Combina: `[EVENTO][TIPO][8 ALEATORIOS]`
5. Verifica que el código sea único
6. Genera un hash SHA-256 para `secret_hash`

**Ejemplo de código generado:**
```
TECSP12345678  ← TE = Evento, CS = Tipo, P12345678 = Aleatorio
```

**Observaciones:**
- Solo se ejecuta en INSERT
- Incluye mecanismo de reintentos (máximo 10 intentos)
- Genera automáticamente el `secret_hash`
- Usa `gen_random_bytes()` de la extensión `pgcrypto`

---

#### 4. `ticketing.check_ticket_availability()`

**Schema:** `ticketing`

**Propósito:** Verifica la disponibilidad de tickets para un tipo específico y cantidad solicitada.

**Definición:**

```sql
CREATE OR REPLACE FUNCTION ticketing.check_ticket_availability(
    p_ticket_type_id BIGINT,
    p_quantity INTEGER
)
RETURNS JSONB AS $$
DECLARE
    v_ticket_type RECORD;
    v_available INTEGER;
    v_result JSONB;
BEGIN
    SELECT 
        *,
        (total_quantity - sold_quantity - reserved_quantity) as available_qty
    INTO v_ticket_type
    FROM ticketing.ticket_types
    WHERE id = p_ticket_type_id
    FOR UPDATE SKIP LOCKED;
    
    IF NOT FOUND THEN
        RETURN jsonb_build_object(
            'available', false,
            'reason', 'Ticket type not found'
        );
    END IF;
    
    v_available := v_ticket_type.available_qty;
    
    IF NOT v_ticket_type.is_active THEN
        v_result := jsonb_build_object(
            'available', false,
            'reason', 'Ticket type is inactive'
        );
    ELSIF v_ticket_type.sale_starts_at > NOW() THEN
        v_result := jsonb_build_object(
            'available', false,
            'reason', 'Sale has not started'
        );
    ELSIF v_ticket_type.sale_ends_at IS NOT NULL AND v_ticket_type.sale_ends_at < NOW() THEN
        v_result := jsonb_build_object(
            'available', false,
            'reason', 'Sale has ended'
        );
    ELSIF v_available <= 0 THEN
        v_result := jsonb_build_object(
            'available', false,
            'reason', 'Sold out'
        );
    ELSIF v_available < p_quantity THEN
        v_result := jsonb_build_object(
            'available', false,
            'reason', 'Not enough tickets available',
            'available_quantity', v_available
        );
    ELSIF p_quantity > v_ticket_type.max_per_order THEN
        v_result := jsonb_build_object(
            'available', false,
            'reason', 'Exceeds maximum per order'
        );
    ELSIF p_quantity < v_ticket_type.min_per_order THEN
        v_result := jsonb_build_object(
            'available', false,
            'reason', 'Below minimum per order'
        );
    ELSE
        v_result := jsonb_build_object(
            'available', true,
            'available_quantity', v_available,
            'ticket_type', jsonb_build_object(
                'id', v_ticket_type.id,
                'name', v_ticket_type.name,
                'price', v_ticket_type.base_price,
                'currency', v_ticket_type.currency
            )
        );
    END IF;
    
    RETURN v_result;
END;
$$ LANGUAGE plpgsql;
```

**Parámetros de entrada:**

| Parámetro | Tipo | Descripción |
|-----------|------|-------------|
| `p_ticket_type_id` | BIGINT | ID del tipo de ticket |
| `p_quantity` | INTEGER | Cantidad solicitada |

**Retorno:**

```json
{
    "available": true/false,
    "reason": "Motivo de disponibilidad o error",
    "available_quantity": 150,
    "ticket_type": {
        "id": 123,
        "name": "VIP",
        "price": 2500.00,
        "currency": "MXN"
    }
}
```

**Validaciones:**

| Orden | Validación | Mensaje |
|-------|------------|---------|
| 1 | Existe el tipo de ticket | "Ticket type not found" |
| 2 | Está activo | "Ticket type is inactive" |
| 3 | Fecha de inicio de venta | "Sale has not started" |
| 4 | Fecha de fin de venta | "Sale has ended" |
| 5 | Disponibilidad > 0 | "Sold out" |
| 6 | Disponibilidad >= cantidad | "Not enough tickets available" |
| 7 | Cantidad <= max_per_order | "Exceeds maximum per order" |
| 8 | Cantidad >= min_per_order | "Below minimum per order" |
| 9 | Todas las validaciones OK | **Disponible** |

**Observaciones:**
- Retorna JSONB para fácil consumo en APIs
- Incluye `FOR UPDATE SKIP LOCKED` para concurrencia
- Valida todas las reglas de negocio en un solo lugar
- Reutilizable desde el Gateway

---

### Análisis de funciones

| Aspecto | Evaluación |
|---------|------------|
| **Funciones de utilidad** | `update_updated_at()` duplicada en 2 schemas ⚠️ |
| **Función de negocio** | `generate_ticket_code()` bien diseñada ✅ |
| **Función de validación** | `check_ticket_availability()` robusta ✅ |
| **Uso de extensiones** | `gen_random_bytes()` de `pgcrypto` ✅ |
| **Manejo de concurrencia** | `SKIP LOCKED` en `check_ticket_availability` ✅ |

---

### Recomendaciones

```text
1. Consolidar `update_updated_at()` en un solo schema:
   - Eliminar `public.update_updated_at()`
   - Usar solo `auth.update_updated_at()`
   - Actualizar triggers que usan `public.update_updated_at()` para que usen `auth.update_updated_at()`

2. Verificar rendimiento de `check_ticket_availability`:
   - Evaluar si `FOR UPDATE SKIP LOCKED` es necesario en todos los casos
   - Considerar si el bloqueo es apropiado para alta concurrencia

3. Considerar auditoría de `generate_ticket_code`:
   - El código generado incluye prefijos del evento y tipo
   - Verificar que la aleatoriedad sea suficiente para evitar colisiones
```